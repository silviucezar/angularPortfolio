https://stackoverflow.com/questions/54856979/angular-typescript-interface-default-value

    function getDeviceType() {
        function checkApproxAspectRatio(idealAR) {
            var AR = window.innerWidth / window.innerHeight;
            var maxAR = AR * 1.05;
            var minAR = AR - (maxAR - AR);
            return minAR <= idealAR && idealAR <= maxAR;
        }
        switch (true) {
            case (window.innerHeight < window.innerWidth) && (checkApproxAspectRatio(1.33) || checkApproxAspectRatio(1.34)):
                return "tabletLandscape";
            case (window.innerHeight > window.innerWidth) && (checkApproxAspectRatio(0.75) || checkApproxAspectRatio(0.75)):
                return "tabletPortrait";
            case(window.innerHeight < window.innerWidth) && (checkApproxAspectRatio(2) || checkApproxAspectRatio(1.77) || checkApproxAspectRatio(1.61)) && Boolean(navigator.userAgent.toLowerCase().match(/android|iphone/gi)):
                return "phoneLandscape";
            case (window.innerHeight > window.innerWidth) && (checkApproxAspectRatio(0.49) || checkApproxAspectRatio(0.56) || checkApproxAspectRatio(0.63)) && Boolean(navigator.userAgent.toLowerCase().match(/android|iphone/gi)):
                return "phonePortrait";
            case(window.innerHeight < window.innerWidth) && (checkApproxAspectRatio(1.78) || checkApproxAspectRatio(1.77) || checkApproxAspectRatio(1.6) || (checkApproxAspectRatio(2))):
                return "desktopLandscape";
            case (window.innerHeight > window.innerWidth) && (checkApproxAspectRatio(0.56) || checkApproxAspectRatio(0.625)):
                return "desktopPortrait";
            case(window.innerHeight < window.innerWidth) && checkApproxAspectRatio(1.25):
                return "desktopLandscape54";
            case(window.innerHeight > window.innerWidth) && checkApproxAspectRatio(0.8):
                return "desktopPortrait54";
            case(window.innerHeight < window.innerWidth) && checkApproxAspectRatio(1.5):
                return "desktopLandscape32";
            case(window.innerHeight > window.innerWidth) && checkApproxAspectRatio(0.66):
                return "desktopPortrait32";
            case(window.innerHeight < window.innerWidth) && (checkApproxAspectRatio(2.33) || checkApproxAspectRatio(2.37) || checkApproxAspectRatio(2.38)):
                return "desktopLandscape219";
            case(window.innerHeight > window.innerWidth) && (checkApproxAspectRatio(0.42) || checkApproxAspectRatio(0.41) || checkApproxAspectRatio(0.418)):
                return "desktopPortrait219";
            case(window.innerHeight < window.innerWidth) && checkApproxAspectRatio(3.55):
                return "desktopLandscape329";
            case(window.innerHeight > window.innerWidth) && checkApproxAspectRatio(0.28):
                return "desktopPortrait219";
        }
    }